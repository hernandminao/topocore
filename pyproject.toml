# SciPy is mandatory because topocore.processing.ground.pmf imports
# scipy.ndimage eagerly.

[project]
name = "topocore"
version = "0.1.0"
description = "Professional Computational Geometry and Topographic Engine"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "Hernán Mina" }
]

dependencies = [
    "numpy>=2.3.0",
    "laspy[lazrs]>=2.5",
    "scipy>=1.10,<2.0",
    "shapely>=2.1.0",
    "pyproj>=3.7.0",
]

# CSF remains optional and is imported only when method="csf" is used.
[project.optional-dependencies]

terrain = [
    "cloth-simulation-filter>=1.1.7",
]

ml = [
    "scikit-learn>=1.7.0",
    "joblib>=1.4.0",
]

lightgbm = [
    "lightgbm>=4.0",
]

xgboost = [
    "xgboost>=2.0",
]

full = [
    "cloth-simulation-filter>=1.1.7",
    "scikit-learn>=1.7.0",
    "joblib>=1.4.0",
    "lightgbm>=4.0",
    "xgboost>=2.0",
]

[dependency-groups]
dev = [
    "hypothesis>=6.155.7",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.6",
    "mypy>=2.1.0",
    "pre-commit>=4.6.0",
    "pytest>=9.1.1",
    "pytest-benchmark>=5.2.3",
    "pytest-cov>=7.1.0",
    "ruff>=0.15.20",

    # Type stubs
    "scipy-stubs>=1.16.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["E501"]

# -----------------------------------------------------------------------------
# MyPy
# -----------------------------------------------------------------------------

[tool.mypy]
python_version = "3.12"
strict = false  # <-- Cambiar a false
warn_unused_configs = true
warn_return_any = true
warn_unreachable = true
warn_redundant_casts = true
warn_unused_ignores = true
show_error_codes = true
pretty = true

# En lugar de strict, activar reglas individuales
disallow_any_unimported = false
disallow_any_expr = false
disallow_any_decorated = false
disallow_any_explicit = false
disallow_any_generics = false
disallow_subclassing_any = false

[[tool.mypy.overrides]]
module = [
    "sklearn",
    "sklearn.*",
    "joblib",
    "joblib.*",
]

follow_untyped_imports = true
follow_imports = "skip"

[[tool.mypy.overrides]]
module = [
    "shapely",
    "shapely.*",
    "laspy",
    "laspy.*",
    "pyproj",
    "pyproj.*",
    "scipy",
    "scipy.*",
    "scipy.spatial",
    "scipy.spatial.*",
    "numpy",
    "numpy.*",
]
ignore_missing_imports = true
follow_imports = "skip"

[[tool.mypy.overrides]]
module = [
    "lightgbm",
    "lightgbm.*",
    "xgboost",
    "xgboost.*",
]
ignore_missing_imports = true

