[pytest]
filterwarnings =
    ignore::DeprecationWarning:joblib.numpy_pickle

testpaths = tests

pythonpath = src .

addopts =
    -ra
    --strict-markers
    --cov=src/topocore
    --cov-report=term-missing